---
description: 任务分配、风险分级、审核、自检和主动推进流程
alwaysApply: true
---

# 任务生命周期规则

## 默认流程

- 默认将任务分配给最匹配的 subagent 执行，主 agent 负责分发、范围控制、汇总和最终回复。
- 简单低风险任务可走：意图确认 -> 成员执行 -> 成员自检 -> PM 按需审核 -> 提交推送。
- 版本规划、多步骤任务、跨职能任务和发布任务必须由 PM 介入。
- 任务进入执行前，应明确验收标准和最小验证计划；涉及可玩内容时，应优先包含浏览器运行、日志、Playwright 或等价自动化反馈。
- 每个成员完成自己的任务后至少要自检，并说明修改范围、验证方式和残留风险。
- 任务提交粒度按成员划分：每个成员完成自己的任务后提交一次。

## 高风险任务

以下项目风险任务必须按高风险流程处理，需要专项审核、PM 判断和制作人最终决定后才能提交：

- 核心玩法、系统规则、数值框架、存档和关键数据结构。

项目高风险流程：制作人确定方向 -> PM 拆分 -> 成员执行 -> 专项成员审核 -> PM 汇总审核 -> 制作人决定 -> 提交推送。

高风险任务必须在执行前写清验证计划，完成后至少提供两类反馈来源，例如脚本/自动化验证与浏览器/实机体验验证；验证不足时不得直接进入提交、发布或制作人终验。

涉及本机环境的危险操作必须单独处理，包括软件安装、系统配置、全局环境、删除文件、覆盖文件、权限提升、驱动安装、系统服务修改等。这类操作必须先说明风险并等待用户确认，不能只由制作人决定。

## 主动推进

- 项目应由 agent 自主持续推进；除非用户主动终止，否则完成一个任务或版本后，应继续进入下一阶段。
- 当没有用户新指令时，制作人应参考 `42.md` 或自主决定方向。
- PM 根据制作人方向拆分任务，并选择合适成员执行。
- 团队可以在遵守分配、审核、自检、提交和推送规则的前提下主动修改代码、资源或文档。
- 涉及项目高风险任务时，必须先按项目高风险流程处理；涉及本机环境危险操作时，必须先获得用户确认。

## 任务记录

- PM 在 `Workspaces/game-project-manager/` 中使用 Markdown 看板记录任务。
- 看板状态由 PM 根据复杂度决定，至少要能看出待办、进行中、待审核、完成和阻塞。
- 任务记录应包含负责人、目标、允许修改范围、验收标准、验证计划、验证结果、当前状态和风险。
- PM 审核时必须判断验证结果是否足以支撑验收；若只完成代码修改但缺少必要运行反馈，应退回补测或记录明确残留风险。
